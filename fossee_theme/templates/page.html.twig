{#
/**
 * @file
 * FOSSEE theme implementation to display a single page.
 */
#}
<div class="layout-container">
  
  {# Top Banner Section #}
  {% if page.top_banner %}
    <div class="top-banner-section">
      <div class="container">
        {{ page.top_banner }}
      </div>
    </div>
  {% endif %}

  {# Header Section #}
  <header class="header-section" role="banner">
    <div class="container">
      <div class="row align-items-center">
        <div class="col-md-6">
          {% if logo %}
            <a href="{{ path('<front>') }}" title="{{ 'Home'|t }}" rel="home" class="logo">
              <img src="{{ logo }}" alt="{{ 'Home'|t }}" class="img-fluid" />
            </a>
          {% endif %}
          {% if site_name %}
            <h1 class="site-name">
              <a href="{{ path('<front>') }}" title="{{ 'Home'|t }}" rel="home">{{ site_name }}</a>
            </h1>
          {% endif %}
          {% if site_slogan %}
            <div class="site-slogan">{{ site_slogan }}</div>
          {% endif %}
        </div>
        <div class="col-md-6">
          {{ page.header }}
        </div>
      </div>
    </div>
  </header>

  {# Navigation Section #}
  {% if page.navigation %}
    <nav class="navigation-section" role="navigation">
      <div class="container">
        {{ page.navigation }}
      </div>
    </nav>
  {% endif %}

  {# Hero Section #}
  {% if page.hero_section or hero_slider_images %}
    <section class="hero-section">
      <div class="container-fluid">
        <div class="hero-slider">
          {% if hero_slider_images %}
            {% for image in hero_slider_images %}
              <div class="hero-slide">
                <img src="{{ image.url }}" alt="{{ image.alt }}" class="img-fluid" />
              </div>
            {% endfor %}
          {% endif %}
          {{ page.hero_section }}
        </div>
      </div>
    </section>
  {% endif %}

  {# Main Content Area #}
  <main class="main-content" role="main">
    <div class="container">
      <div class="row">
        
        {# News and Notice Section #}
        {% if page.news_notice %}
          <div class="col-md-8">
            <section class="news-notice-section">
              <h2>{{ 'Current News'|t }}</h2>
              {{ page.news_notice }}
            </section>
          </div>
        {% endif %}

        {# Main Content #}
        <div class="col-md-{{ page.news_notice ? '4' : '12' }}">
          {{ page.content }}
        </div>
      </div>
    </div>
  </main>

  {# Activity Section #}
  {% if page.activity_section %}
    <section class="activity-section">
      <div class="container">
        <div class="row">
          <div class="col-md-12">
            {{ page.activity_section }}
          </div>
        </div>
      </div>
    </section>
  {% endif %}

  {# Features Section #}
  {% if page.features_section %}
    <section class="features-section">
      <div class="container">
        {{ page.features_section }}
      </div>
    </section>
  {% endif %}

  {# Footer Section #}
  {% if page.footer %}
    <footer class="footer-section" role="contentinfo">
      <div class="container">
        <div class="row">
          <div class="col-md-4">
            <div class="footer-logos">
              {% if iitb_logo_url %}
                <div class="footer-logo iitb-logo">
                  <img src="{{ iitb_logo_url }}" alt="IIT Bombay" class="img-fluid" />
                </div>
              {% endif %}
              {% if fossee_logo_url %}
                <div class="footer-logo fossee-logo">
                  <img src="{{ fossee_logo_url }}" alt="FOSSEE" class="img-fluid" />
                </div>
              {% endif %}
            </div>
          </div>
          <div class="col-md-4">
            {{ page.footer }}
          </div>
          <div class="col-md-4">
            {% if show_social_links %}
              <div class="social-links">
                <h4>{{ 'FOSSEE Social Media'|t }}</h4>
                <div class="social-icons">
                  <a href="{{ facebook_url }}" class="social-icon facebook" title="Facebook">
                    <i class="fab fa-facebook"></i>
                  </a>
                  <a href="{{ youtube_url }}" class="social-icon youtube" title="YouTube">
                    <i class="fab fa-youtube"></i>
                  </a>
                  <a href="{{ linkedin_url }}" class="social-icon linkedin" title="LinkedIn">
                    <i class="fab fa-linkedin"></i>
                  </a>
                  <a href="{{ instagram_url }}" class="social-icon instagram" title="Instagram">
                    <i class="fab fa-instagram"></i>
                  </a>
                </div>
              </div>
            {% endif %}
          </div>
        </div>
        <div class="row">
          <div class="col-md-12">
            <div class="footer-bottom">
              <p>&copy; {{ "now"|date("Y") }} {{ footer_text }}</p>
            </div>
          </div>
        </div>
      </div>
    </footer>
  {% endif %}

</div>